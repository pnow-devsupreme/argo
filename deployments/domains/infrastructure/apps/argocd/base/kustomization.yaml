apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: argocd-config
resources:
  - ingress.yaml
  - clusterissuer.yaml
  - certificate.yaml
# patches:
#   patch:
#     - path: ./patches
