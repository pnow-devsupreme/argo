apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-server
  namespace: argocd
spec:
  template:
    spec:
      containers:
      - name: argocd-server
        volumeMounts:
        - mountPath: /app/config/tls
          name: tls-certs
      volumes:
      - name: tls-certs
        secret:
          secretName: argocd-server-tls