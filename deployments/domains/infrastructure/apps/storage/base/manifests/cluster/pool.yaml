apiVersion: ceph.rook.io/v1
kind: CephBlockPool
metadata:
  name: replicapool
  namespace: rook-ceph
spec:
  failureDomain: host
  replicated:
    size: 1 # Since we're running on a single node
    requireSafeReplicaSize: false # Required for single-node deployments
