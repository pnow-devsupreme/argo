# File: deployments/bootstrap/virtual-kubelet/base/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: virtual-kubelet-config
  namespace: virtual-kubelet-system
data:
  provider.conf: |
    {
      "provider": "mock",
      "nodeName": "virtual-node",
      "disableResourceRequests": false,
      "enableResourceLimits": true
    }
